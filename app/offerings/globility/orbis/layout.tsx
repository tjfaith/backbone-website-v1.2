export const metadata = {
  title: "Orbis Globility Offerings",
  description: "Our offerings for orbis globility",
};

export default function BusinessesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
