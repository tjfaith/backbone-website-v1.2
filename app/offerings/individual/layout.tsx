export const metadata = {
  title: "Individual Offerings",
  description: "Our offerings for individuals",
};

export default function IndividualLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
