export const metadata = {
  title: "Investments Offerings",
  description: "Our offerings for investments",
};

export default function BusinessesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
