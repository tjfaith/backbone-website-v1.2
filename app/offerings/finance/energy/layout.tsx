export const metadata = {
  title: "Energy Finance Offerings",
  description: "Our offerings for energy finance",
};

export default function BusinessesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
