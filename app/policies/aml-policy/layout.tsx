export const metadata = {
  title: "AML Policy",
  description: "Read our aml policy",
};

export default function BusinessesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
