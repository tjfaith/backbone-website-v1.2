export const metadata = {
  title: "Service Terms Policy",
  description: "Read our service terms policy",
};

export default function BusinessesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
